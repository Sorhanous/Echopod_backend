Run Local:
1. Build: docker build -t bevi_backend .
2. Run: docker run -p 8080:8080 bevi_backend



Deploy to GCP:
1. Build: `docker buildx build --platform linux/amd64 -t gcr.io/dynamic-heading-419922/bevi_backend:v1 --load .`
2. Push: `docker push gcr.io/dynamic-heading-419922/bevi_backend:v1`
3. Deploy: `gcloud run deploy bevi-backend --image gcr.io/dynamic-heading-419922/bevi_backend:v1 --platform managed --region us-east1 --allow-unauthenticated`
4. Run:  `gcloud run services list --platform managed`